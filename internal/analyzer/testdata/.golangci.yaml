version: "2"

issues:
  max-same-issues: 0

linters:
  default: none
  enable:
    - logcheck
  settings:
    custom:
      logcheck:
        type: "module"
        settings:
          enabled-rules: ["capitalize", "symbols", "sensitive"]
          sensitive-words: ["token", "bearer"]
          allowed-symbols: "%"